import Foundation

public let heavyCompile1: (Array<Int>) -> Array<Int> = { inputs in
    return inputs
        .map { String($0) }
        .compactMap { Int($0) }
        .map { String($0) }
        .compactMap { Int($0) }
}

public let heavyCompile2: (Array<Int>) -> Array<Int> = { inputs in
    return inputs
        .map { String($0) }
        .compactMap { Int($0) }
        .map { String($0) }
        .compactMap { Int($0) }
}

public let heavyCompile3: (Array<Int>) -> Array<Int> = { inputs in
    return inputs
        .map { String($0) }
        .compactMap { Int($0) }
        .map { String($0) }
        .compactMap { Int($0) }
}

public let heavyCompile4: (Array<Int>) -> Array<Int> = { inputs in
    return inputs
        .map { String($0) }
        .compactMap { Int($0) }
        .map { String($0) }
        .compactMap { Int($0) }
}

public let heavyCompile5: (Array<Int>) -> Array<Int> = { inputs in
    return inputs
        .map { String($0) }
        .compactMap { Int($0) }
        .map { String($0) }
        .compactMap { Int($0) }
}

public let heavyCompile6: (Array<Int>) -> Array<Int> = { inputs in
    return inputs
        .map { String($0) }
        .compactMap { Int($0) }
        .map { String($0) }
        .compactMap { Int($0) }
}

public let heavyCompile7: (Array<Int>) -> Array<Int> = { inputs in
    return inputs
        .map { String($0) }
        .compactMap { Int($0) }
        .map { String($0) }
        .compactMap { Int($0) }
}

public let heavyCompile8: (Array<Int>) -> Array<Int> = { inputs in
    return inputs
        .map { String($0) }
        .compactMap { Int($0) }
        .map { String($0) }
        .compactMap { Int($0) }
}
